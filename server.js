// In your server's main file (e.g., index.js or server.js)
const PORT = process.env.PORT || 8080;
// ... setup your http server ...
server.listen(PORT, () => console.log(`Server listening on port ${PORT}`));
